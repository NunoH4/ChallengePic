<% flash.each do |key, value| %>
  <p class="alert alert-<%= bootstrap_alert(key) ? bootstrap_alert(key) : key %> alert-dismissible fade show">
    <%= value %><button type="button" class="close" data-dismiss="alert">&times;</button>
  </p>
<% end %>