<% if model.errors.any? %>
  <div class="alert alert-warning">
    <h4><%= pluralize(model.errors.count, "error") %> prohibited this post from being saved:</h4>
    <ul>
    <% model.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
    </ul>
  </div>
<% end %>